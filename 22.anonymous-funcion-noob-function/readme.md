# Go Function Expression (Noob Guide)

এই README ফাইলটি একদম নতুন (noob) Go শেখার জন্য লেখা। এখানে **Function Expression** কী, কেন ব্যবহার করা হয়, এবং একটি খুব সহজ উদাহরণ দেওয়া হয়েছে।

---

## Function Expression কী?

Go-তে সাধারণত আমরা ফাংশন এভাবে লিখি:

```go
func add(a int, b int) int {
    return a + b
}
```

কিন্তু **Function Expression** মানে হলো—
👉 ফাংশনকে একটি **variable-এর মধ্যে রেখে ব্যবহার করা**।

সহজ ভাষায়:

> ফাংশনও Go-তে একটা value, ঠিক integer বা string এর মতো।

---

## Noob-Friendly Example 🚀

```go
package main

import "fmt"

func main() {

    // function expression
    add := func(a int, b int) int {
        return a + b
    }

    result := add(10, 20)
    fmt.Println(result)
}
```

---

## Line by Line Explanation (Bangla)

### 1️⃣ `add := func(a int, b int) int { ... }`

* এখানে `add` হলো একটি **variable**
* `func(a int, b int) int` হলো একটি **anonymous function** (নাম নাই)
* এই ফাংশনটা `add` variable-এর ভিতরে রাখা হয়েছে

👉 তাই একে বলে **Function Expression**

---

### 2️⃣ `return a + b`

* ফাংশনটি দুইটা সংখ্যা যোগ করে রিটার্ন করছে

---

### 3️⃣ `result := add(10, 20)`

* এখানে `add` কে আমরা ফাংশনের মতো কল করছি
* Go বুঝে যাচ্ছে `add` এর ভিতরে একটি ফাংশন আছে

---

### 4️⃣ `fmt.Println(result)`

* আউটপুট দেখাবে:

```
30
```

---

## Normal Function vs Function Expression

| Normal Function     | Function Expression           |
| ------------------- | ----------------------------- |
| আলাদা করে নাম থাকে  | variable-এর মধ্যে থাকে        |
| আগেই define করতে হয় | যেখানে দরকার সেখানেই লেখা যায় |
| Reusable            | Flexible & dynamic            |

---

## কেন Function Expression ব্যবহার করবো?

✔️ Code বেশি flexible হয়
✔️ Callback function লিখতে সুবিধা
✔️ Closure তৈরি করা যায়
✔️ Clean & modern Go style

---

## One-Line Summary 🧠

> Go-তে যখন কোনো function কে variable-এর মধ্যে রেখে ব্যবহার করা হয়, তখন সেটাকে **Function Expression** বলে।

---

Happy Coding with Go ❤️



# Go Function Expression & init() — Noob Friendly Guide 🇧🇩

এই ডকুমেন্টে দেওয়া Go কোডটি একদম **beginner / noob-friendly** ভাবে ব্যাখ্যা করা হয়েছে। এখানে তুমি শিখবে:

* `init()` ফাংশন কী
* Function Expression কী
* Global vs Local function expression
* Go program এর execution order

---

## 🧾 Original Code

```go
package main

import "fmt"

func init() {
	fmt.Println("Init")
}

var myCounterFunc = func(maxNumb int) {
	for i := 0; i <= maxNumb; i++ {
		fmt.Println(i)
	}
}

func main() {
	noob := func(b, c int) {
		fmt.Println(b + c)
	}

	noob(10, 20)
	myCounterFunc(100)
}
```

---

## 🔹 Overall Program Idea (Big Picture)

এই প্রোগ্রামটি দেখায়:

* Go-তে `init()` কীভাবে **main() এর আগে** রান হয়
* কীভাবে ফাংশনকে variable-এর ভিতরে রাখা যায়
* কীভাবে সেই variable-কে পরে function হিসেবে call করা যায়

---

## 1️⃣ `init()` Function — কেন আগে রান হয়?

```go
func init() {
	fmt.Println("Init")
}
```

### 🔍 Bangla Explanation

* `init()` হলো Go-এর একটি **special function**
* এটাকে কখনো manually call করা হয় না
* Go runtime নিজে থেকেই এটাকে চালায়

📌 **Rule:**

```
init() → main()
```

তাই প্রোগ্রাম রান করলেই সবার আগে output আসবে:

```
Init
```

---

## 2️⃣ Global Function Expression

```go
var myCounterFunc = func(maxNumb int) {
	for i := 0; i <= maxNumb; i++ {
		fmt.Println(i)
	}
}
```

### 🔍 এখানে কী হচ্ছে?

* `myCounterFunc` হলো একটি **global variable**
* variable-এর ভিতরে রাখা হয়েছে একটি **anonymous function**
* একেই বলা হয় 👉 **Function Expression**

### 📌 ফাংশনের কাজ

* `0` থেকে `maxNumb` পর্যন্ত সংখ্যা প্রিন্ট করা

👉 এখানে ফাংশনটা শুধু **define** করা হয়েছে, এখনো **execute** হয়নি

---

## 3️⃣ `main()` এর ভিতরের Function Expression (Local)

```go
noob := func(b, c int) {
	fmt.Println(b + c)
}
```

### 🔍 Explanation

* `noob` হলো একটি **local variable**
* এর ভিতরে রাখা হয়েছে একটি anonymous function
* এই ফাংশন দুইটা সংখ্যা যোগ করে প্রিন্ট করে

📌 Scope:

* এই ফাংশন শুধু `main()` এর ভিতরেই কাজ করবে

---

## 4️⃣ Function Call করা

### ▶️ Local Function Call

```go
noob(10, 20)
```

**Output:**

```
30
```

কারণ:

```
10 + 20 = 30
```

---

### ▶️ Global Function Call

```go
myCounterFunc(100)
```

**Output:**

```
0
1
2
...
100
```

---

## 5️⃣ Program Execution Order (Very Important 🧠)

Go প্রোগ্রাম রান হলে sequence হবে 👇

1. `init()` রান হবে
2. `Init` প্রিন্ট হবে
3. `main()` শুরু হবে
4. `noob(10, 20)` → `30`
5. `myCounterFunc(100)` → `0` থেকে `100`

---

## 🖨️ Final Output (Summary)

```
Init
30
0
1
2
...
100
```

---

## ✅ Key Takeaways (Noob Notes)

* ✔️ `init()` নিজে নিজে রান হয়
* ✔️ Function কে variable-এর ভিতরে রাখলে তাকে **Function Expression** বলে
* ✔️ Global function expression সব জায়গা থেকে call করা যায়
* ✔️ Local function expression শুধু নিজের scope-এ কাজ করে

---

## 🧠 One-Line Summary

> Go-তে function যদি variable-এর ভিতরে রাখা হয় এবং পরে সেই variable দিয়ে call করা হয়, সেটাই Function Expression।

---

Happy Coding with Go ❤️
