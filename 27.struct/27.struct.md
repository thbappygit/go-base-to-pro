# 🧠 Go Struct 

---

## 🔰 Struct কী? (এক লাইনে)

**Struct হলো Go-এর একটি user-defined data type, যেটা দিয়ে একসাথে একাধিক related ডেটা রাখা যায়।**

👉 বাস্তব জীবনের কোনো বস্তু / entity কে কোডে প্রকাশ করতেই Struct ব্যবহার করা হয়।

---

## 🌍 বাস্তব জীবনের সাথে মিল

ধরা যাক **Country (দেশ)** — একটি দেশ বলতে আমরা সাধারণত যা বুঝি:

* নাম (name)
* জনসংখ্যা (population)
* ফোন কোড (phone code)

👉 এগুলো আলাদা আলাদা variable না বানিয়ে একসাথে রাখাই Struct।

---

## 🧩 Struct ঘোষণা (Declaration)

```go
type Country struct {
	name       string
	population int
	phoneCode  int
}
```

### এখানে কী হচ্ছে?

* `Country` → Struct-এর নাম
* `name`, `population`, `phoneCode` → Struct-এর field
* প্রতিটি field-এর নিজস্ব data type আছে

📌 **Struct = একটি container, field = তার ভেতরের ডেটা**

---

## 🏗️ Struct ভ্যারিয়েবল তৈরি (Instantiation)

### 1️⃣ Zero-value সহ Struct তৈরি

```go
var country1 Country
```

এখানে:

* `country1` তৈরি হয়েছে
* কিন্তু field গুলোতে default value বসে আছে

Default values:

* string → `""`
* int → `0`

---

### 2️⃣ Struct literal দিয়ে মান বসানো

```go
country1 = Country{
	name:       "China",
	population: 1400000000,
	phoneCode:  91,
}
```

📌 **Field নাম লিখে value দিলে order matter করে না**

---

### 3️⃣ Short-hand পদ্ধতি (সবচেয়ে বেশি ব্যবহার হয়)

```go
country2 := Country{
	name:       "Bangladesh",
	population: 160000000,
	phoneCode:  880,
}
```

---

## 🔍 Struct এর Field access করা

```go
country1.name
country1.population
country1.phoneCode
```

👉 Dot (`.`) ব্যবহার করে Struct-এর ভেতরের field ধরা হয়

---

## 🖨️ Output দেখানো

```go
fmt.Println("1st country : ", country1.name, " 2nd country : ", country2.name)
fmt.Println("1st country total populations : ", country1.population,
            " 2nd country total populations : ", country2.population)
```

🧠 মনে রাখো:

> Struct নিজে কিছু না, তার ভেতরের field-ই আসল ডেটা

---

## 🚀 `init()` ফাংশন কী এবং কেন আগে চলে?

```go
func init() {
	fmt.Println("Welcome to struct learning")
}
```

### init() সম্পর্কে স্থায়ী নিয়ম

* `main()` এর আগে execute হয়
* Automatically call হয়
* একবারই চলে

Execution flow:

```
Program start → init() → main()
```

---

## 🧠 Struct মনে রাখার Golden Formula

```
Struct = Real World Object
Field  = Object-এর Property
```

উদাহরণ:

```
Country
 ├─ name
 ├─ population
 └─ phoneCode
```

---

## ✅ Struct কখন ব্যবহার করবে?

✔️ একাধিক related ডেটা একসাথে রাখতে চাইলে
✔️ কোড readable ও clean করতে চাইলে
✔️ Real-world entity represent করতে চাইলে
✔️ API model, Database row, JSON structure বানাতে

---

## 💎 এক লাইনে শেষ কথা

> **Go Struct হলো data কে সুন্দর, পরিষ্কার ও বাস্তবসম্মতভাবে organize করার সবচেয়ে শক্তিশালী উপায়**

---


এই ফাইলটা বারবার পড়লে Struct ধারণাটা মাথায় গেঁথে যাবে 💙
