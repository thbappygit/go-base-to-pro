# Go Slice এর Length এবং Capacity ব্যাখ্যা

## প্রথম কোড দেখি

```go
highScores := make([]int, 5, 7)
```

এর মানে:
- **length = 5** → এখন ৫টা element আছে
- **capacity = 7** → মোট জায়গা আছে ৭টা element রাখার

### 📦 একটা বাক্স হিসেবে চিন্তা করো:
- ৫টা ঘর ভর্তি
- আর ২টা ঘর ফাঁকা

---

## এখন append হচ্ছে

```go
highScores = append(highScores, 782, 889, 698)
```

তুমি যোগ করছো **৩টা** নতুন element।

কিন্তু ❗ সমস্যা:
- বর্তমান capacity ছিল **৭**
- আগে ছিল **৫**
- ফাঁকা ছিল **২**
- ➡️ ৩টা ঢুকাতে জায়গা কম

---

## তখন Go কী করে?

Go বলে:
> "পুরনো array ছোট, আমি নতুন একটা বড় array বানাবো"

### 🔁 নতুন array তৈরি হয়

Go সাধারণত পুরনো capacity **× 2** করে (ছোট slice হলে)

আগে capacity ছিল **7**  
নতুন capacity হয়:

```
7 × 2 = 14
```

---

## তাই final output

```go
fmt.Println(highScores, len(highScores), cap(highScores))
```

### 📌 Result:
- **length = 8** (৫ + ৩)
- **capacity = 14** ← এইটাই তোমার প্রশ্নের উত্তর

---

## সহজ ভাষায় সংক্ষেপে

✅ শুরুতে:
```
len = 5
cap = 7
```

✅ append করার সময় জায়গা কম পড়েছে  
✅ Go নতুন বড় array বানিয়েছে  
✅ capacity দ্বিগুণ করেছে → **14**

---

## মনে রাখার ট্রিক 🧠

| অবস্থা | Go কী করে | Capacity |
|--------|-----------|----------|
| capacity এর মধ্যে append | পুরনো array | একই থাকে |
| capacity ছাড়ালে append | নতুন array | ≈ দ্বিগুণ |
| ছোট slice | capacity দ্বিগুণ | × 2 |
| বড় slice | capacity কম বাড়ে | × 1.25 |

---

## আরেকটা উদাহরণ

```go
// শুরু
s := make([]int, 3, 5)  // len=3, cap=5

// ২টা element যোগ
s = append(s, 10, 20)   // len=5, cap=5 (এখনো ফিট করছে)

// আরো ১টা element যোগ
s = append(s, 30)       // len=6, cap=10 (নতুন array, capacity দ্বিগুণ!)
```

এভাবে Go dynamically memory manage করে! 🚀