# GoLang এ Higher Order Function — সম্পূর্ণ বাংলা ব্যাখ্যা

এই ডকুমেন্টে আমরা **Higher Order Function**, **First Order Function**, **Parameter vs Argument**, এবং **Function Return করা**—এই সব কনসেপ্ট পরিষ্কারভাবে বুঝব। উদাহরণ হিসেবে **দুটি Go কোড** ব্যবহার করা হবে।

---

## 🔹 কোড – ১ : Function কে Parameter হিসেবে পাঠানো (Recap)

```go
package main

import "fmt"

// Higher Order Function
func processOperation(x int, y int, operation func(m, n int)) {
	operation(x, y)
}

// First Order Function
func addCalc(p, q int) {
	r := p + q
	fmt.Println(r)
}

func main() {
	processOperation(40, 60, addCalc)
}
```

### 🧠 এখানে কী হচ্ছে?

* `addCalc` একটি **First Order Function**
* `processOperation` একটি **Higher Order Function**
* কারণ `processOperation` একটি ফাংশনকে parameter হিসেবে নিচ্ছে

👉 কলের সময়:

```go
processOperation(40, 60, addCalc)
```

এখানে:

* `40`, `60`, `addCalc` → **Argument**
* `x`, `y`, `operation` → **Parameter**

ভিতরে যা হচ্ছে:

```go
operation(x, y) // মানে addCalc(40, 60)
```

---

## 🔹 কোড – ২ : Function কে Return করা (মূল উদাহরণ)

এখন আমরা দেখব **Higher Order Function যেটা একটি ফাংশন return করে**।

```go
package main

import "fmt"

func callHigherOrderFunc() func(x, y int) {
	return addVals
}

func addVals(x, y int) {
	z := x + y
	fmt.Println(z)
}

func main() {
	sum := callHigherOrderFunc()
	sum(100, 200)
}
```

---

## 🔹 callHigherOrderFunc() — Higher Order Function কেন?

```go
func callHigherOrderFunc() func(x, y int) {
	return addVals
}
```

### 🔍 বিশ্লেষণ:

* এই ফাংশনটি **একটি ফাংশন return করছে**
* return টাইপ:

```go
func(x, y int)
```

👉 অর্থাৎ:

> এই ফাংশন এমন কিছু return করবে যেটা দুইটা `int` নেয়

🔥 তাই `callHigherOrderFunc` = **Higher Order Function**

---

## 🔹 addVals() — First Order Function কেন?

```go
func addVals(x, y int) {
	z := x + y
	fmt.Println(z)
}
```

কারণ:

* এটি শুধু `int` নেয়
* কোনো ফাংশন নেয় না
* কোনো ফাংশন return করে না

👉 তাই এটি **First Order Function**

---

## 🔹 main() এ Step by Step কী হচ্ছে?

```go
sum := callHigherOrderFunc()
```

এখানে:

* `callHigherOrderFunc()` কল হলো
* এটি return করল `addVals`
* `sum` এখন একটি ফাংশন

মানে:

```go
sum == addVals
```

---

```go
sum(100, 200)
```

এখন আসলে কল হচ্ছে:

```go
addVals(100, 200)
```

📤 Output:

```
300
```

---

## 🔹 Parameter vs Argument (এই কোডে)

### 🔸 Parameter

```go
func addVals(x, y int)
```

* `x`, `y` → Parameter

### 🔸 Argument

```go
sum(100, 200)
```

* `100`, `200` → Argument

👉 সহজ ভাষায়:

> Parameter = ফাংশনের ভিতরের নাম
> Argument = বাইরে থেকে পাঠানো মান

---

## 🔹 Real-life Example

ধরো:

* `callHigherOrderFunc` = Manager
* `addVals` = Calculator
* `sum` = Calculator এর remote

Manager বলে:

> “এই calculator টা নাও”

তারপর তুমি remote দিয়ে বলো:

> “100 + 200 করো”

---

## 🔹 Quick Comparison Table

| বিষয়                | First Order Function | Higher Order Function |
| ------------------- | -------------------- | --------------------- |
| Function নেয়        | ❌                    | ✅                     |
| Function return করে | ❌                    | ✅                     |
| উদাহরণ              | addVals              | callHigherOrderFunc   |

---

## 🔹 মনে রাখার Shortcut

* Function যদি **function নেয় বা দেয়** → Higher Order
* শুধু data নিয়ে কাজ করলে → First Order

---

✅ এই কনসেপ্টগুলো বোঝা গেলে:

* Callback
* Closure
* Middleware
* Functional Programming
  সব অনেক সহজ হয়ে যাবে

চাও তো পরের ডকুমেন্টে আমি **Closure + Real Use Case** দেখাতে পারি।
