# Go Programming - Data Types সম্পূর্ণ গাইড

## 📚 সূচিপত্র
- [Integer Data Types](#integer-data-types)
- [Floating Point Data Types](#floating-point-data-types)
- [Boolean Data Type](#boolean-data-type)
- [Character Data Types](#character-data-types)
- [String Data Type](#string-data-type)
- [সম্পূর্ণ উদাহরণ Program](#সম্পূর্ণ-উদাহরণ-program)
- [Printf Format Specifiers](#printf-format-specifiers)

---

## Integer Data Types

### 🔢 Signed Integers (চিহ্নযুক্ত পূর্ণসংখ্যা)
যেসব সংখ্যায় ধনাত্মক (+) এবং ঋণাত্মক (-) উভয় মান থাকতে পারে:

| Type | Bits | Bytes | Range |
|------|------|-------|-------|
| `int8` | 8 | 1 | -128 to 127 |
| `int16` | 16 | 2 | -32,768 to 32,767 |
| `int32` | 32 | 4 | -2,147,483,648 to 2,147,483,647 |
| `int64` | 64 | 8 | -9,223,372,036,854,775,808 to 9,223,372,036,854,775,807 |
| `int` | 32 or 64 | 4 or 8 | কম্পিউটারের আর্কিটেকচার অনুযায়ী |

### ➕ Unsigned Integers (চিহ্নহীন পূর্ণসংখ্যা)
শুধুমাত্র ধনাত্মক সংখ্যা (0 এবং তার উপরে):

| Type | Bits | Bytes | Range |
|------|------|-------|-------|
| `uint8` | 8 | 1 | 0 to 255 |
| `uint16` | 16 | 2 | 0 to 65,535 |
| `uint32` | 32 | 4 | 0 to 4,294,967,295 |
| `uint64` | 64 | 8 | 0 to 18,446,744,073,709,551,615 |
| `uint` | 32 or 64 | 4 or 8 | কম্পিউটারের আর্কিটেকচার অনুযায়ী |

#### 💡 উদাহরণ:
```go
var a int8 = 127        // সর্বোচ্চ মান
var b int8 = -128       // সর্বনিম্ন মান
var c uint8 = 255       // unsigned এর সর্বোচ্চ মান (কোন ঋণাত্মক নেই)
var d uint16 = 1000     // বড় ধনাত্মক সংখ্যা
```

---

## Floating Point Data Types

দশমিক বিন্দুযুক্ত সংখ্যার জন্য ব্যবহৃত হয়:

| Type | Bits | Bytes | Precision |
|------|------|-------|-----------|
| `float32` | 32 | 4 | ~6-7 decimal digits |
| `float64` | 64 | 8 | ~15-16 decimal digits |

#### 💡 উদাহরণ:
```go
var j float32 = 10.2242424  // 32-bit floating point
var k float64 = 29.56       // 64-bit floating point (বেশি accurate)
```

> ⚠️ **নোট:** যদি আপনার কম্পিউটার 32-bit হয়, তাহলে float64 এর জন্য 2টি মেমরি সেল লাগবে (প্রতিটি 32-bit)।

---

## Boolean Data Type

শুধুমাত্র দুইটি মান থাকতে পারে:

| Type | Values | Bits | Bytes |
|------|--------|------|-------|
| `bool` | `true` or `false` | 8 | 1 |

#### 💡 উদাহরণ:
```go
var flag bool = false
var isActive bool = true
```

---

## Character Data Types

### 📝 byte
- `uint8` এর alias
- 8 bits → 1 byte
- একটি ASCII character সংরক্ষণ করে

### 🌍 rune
- `int32` এর alias
- 32 bits → 4 bytes
- একটি Unicode character সংরক্ষণ করে (emoji, বাংলা অক্ষর ইত্যাদি)
- Format: `%c` দিয়ে প্রিন্ট করতে হয়

#### 💡 উদাহরণ:
```go
var letter byte = 'A'    // ASCII character
love := '❤'              // Unicode character (rune)
bangla := 'ক'            // বাংলা character (rune)
```

---

## String Data Type

- একাধিক character এর সমষ্টি
- Format: `%s` দিয়ে প্রিন্ট করা হয়

#### 💡 উদাহরণ:
```go
var name string = "Bangladesh"
message := "হ্যালো ওয়ার্ল্ড"
```

---

## সম্পূর্ণ উদাহরণ Program

```go
package main

import "fmt"

func main() {
    // Integer examples
    var a int8 = 127
    var b int8 = -128
    var c uint8 = 255      // uint মানে unsigned (0 বা ধনাত্মক সংখ্যা)
    var d uint16 = 1000

    // Float examples
    var j float32 = 10.2242424
    var k float64 = 29.56  // 32-bit কম্পিউটারে 2টি cell লাগবে

    // Boolean example
    var flag bool = false

    // Rune example (Unicode character)
    love := '❤'  // একটি character সংরক্ষণ করে

    // সব variable প্রিন্ট করা
    fmt.Println(a, b, c, d, j, k, flag, love)

    // বিভিন্ন format এ প্রিন্ট করা
    fmt.Printf("%c\n", love)   // rune data প্রিন্ট করতে
    fmt.Printf("%d\n", a)      // integer data প্রিন্ট করতে
    fmt.Printf("%.3f\n", j)    // float32 - দশমিকের পর 3 সংখ্যা
    fmt.Printf("%v\n", flag)   // যেকোনো value প্রিন্ট করতে
    fmt.Printf("%T\n", flag)   // variable এর type জানতে
}
```

### 🖥️ Output:
```
127 -128 255 1000 10.2242424 29.56 false 10084
❤
127
10.224
false
bool
```

---

## Printf Format Specifiers

| Format | ব্যবহার | উদাহরণ |
|--------|---------|---------|
| `%c` | Character (rune) প্রিন্ট করতে | `fmt.Printf("%c", 'A')` → A |
| `%d` | Integer (decimal) প্রিন্ট করতে | `fmt.Printf("%d", 100)` → 100 |
| `%f` | Float প্রিন্ট করতে | `fmt.Printf("%f", 3.14)` → 3.140000 |
| `%.2f` | Float এ দশমিকের পর 2 সংখ্যা | `fmt.Printf("%.2f", 3.14159)` → 3.14 |
| `%s` | String প্রিন্ট করতে | `fmt.Printf("%s", "Hello")` → Hello |
| `%v` | যেকোনো value প্রিন্ট করতে | `fmt.Printf("%v", anything)` |
| `%T` | Variable এর type জানতে | `fmt.Printf("%T", 42)` → int |
| `%t` | Boolean প্রিন্ট করতে | `fmt.Printf("%t", true)` → true |
| `%b` | Binary প্রিন্ট করতে | `fmt.Printf("%b", 5)` → 101 |
| `%x` | Hexadecimal প্রিন্ট করতে | `fmt.Printf("%x", 255)` → ff |

---

## 📌 মনে রাখার টিপস

### ✅ কখন কোনটি ব্যবহার করবেন

| পরিস্থিতি | Data Type |
|----------|-----------|
| ছোট পূর্ণসংখ্যা (-128 to 127) | `int8` |
| শুধু ধনাত্মক সংখ্যা (0-255) | `uint8` |
| সাধারণ পূর্ণসংখ্যা | `int` |
| খুব বড় সংখ্যা | `int64` বা `uint64` |
| দশমিক সংখ্যা | `float64` (recommended) |
| সত্য/মিথ্যা | `bool` |
| একটি অক্ষর (ASCII) | `byte` |
| Unicode character (emoji, বাংলা) | `rune` |
| টেক্সট | `string` |

### 🎯 গুরুত্বপূর্ণ পয়েন্ট

1. **int vs uint**:
    - `int` এ ঋণাত্মক সংখ্যা থাকতে পারে
    - `uint` এ শুধু ধনাত্মক সংখ্যা

2. **Memory Efficiency**:
    - ছোট range হলে ছোট datatype ব্যবহার করুন
    - উদাহরণ: age এর জন্য `uint8` যথেষ্ট (0-255)

3. **Float Precision**:
    - সাধারণত `float64` ব্যবহার করুন (বেশি নির্ভুল)
    - `float32` শুধু memory বাঁচানোর জন্য

4. **Unicode Support**:
    - বাংলা, emoji, বিশেষ চিহ্ন → `rune` ব্যবহার করুন
    - সাধারণ ইংরেজি → `byte` যথেষ্ট

---

 

 
---
 