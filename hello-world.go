//Go-তে প্রতিটি ফাইল এক বা একাধিক package এর অংশ।
//main package হচ্ছে entry point package, মানে এই ফাইল রান করলে এখান থেকেই প্রোগ্রাম শুরু হবে।
//যখন আপনি go run main.go চালান, Go প্রথমে package main খুঁজে দেখে এবং তার মধ্যে থাকা func main() থেকে কোড চালায়।
//📌 সংক্ষেপে:
//package main → “এটা executable প্রোগ্রাম, লাইব্রেরি না।”

package main

//🔹 ব্যাখ্যা:
//এটা দিয়ে আমরা Go-এর standard library থেকে "fmt" প্যাকেজ ইমপোর্ট করছি।
//fmt মানে হলো format, এই প্যাকেজে আছে Println, Printf, Sprintf, Sprintln ইত্যাদি ফাংশন — যেগুলো দিয়ে আমরা টেক্সট প্রিন্ট বা ফরম্যাট করতে পারি।
//📌 সংক্ষেপে:
//fmt প্যাকেজ → ইনপুট-আউটপুট, প্রিন্ট করা, ফরম্যাট করা এসব কাজের জন্য ব্যবহৃত হয়।
import "fmt"

// এখানেই আমাদের প্রোগ্রামের execution শুরু হয়।
// Go তে সব executable প্রোগ্রামে একটা main() ফাংশন থাকা লাগে — এটা Laravel এ routes/web.php এর প্রথম request handler এর মতোই।
// 📌 সংক্ষেপে:
// func main() → প্রোগ্রামের শুরু পয়েন্ট (entry point)।
func main() {
	fmt.Println("hello world")
	fmt.Println("Name:", "Habib", "Age:", 25)

	messageOne := fmt.Sprint("Hello Buddy. Welcome to Go Programming Language\n")
	fmt.Print(messageOne)

	messageTwo := fmt.Sprintln("You can learn Go from scratch.")
	fmt.Print(messageTwo)

}
